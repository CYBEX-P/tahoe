import os

"""
Curreny available key to use is installed in my environment variables, it is
a public key and not a premium. Probably need a premium for the final product
"""
API_key = os.envrion["VT_API_KEY"]